# coding=utf-8

SOURCE_BUILD = '13187581'

MIN_PARTS = {
    'action': (2, 2),
    'actionIsDisplay': (1, 1),
    'appear': (1, 5),
    'atlasText': (3, 3),
    'blockProperty': (2, 2),
    'blurLayer': (0, 0),
    'blurMap': (0, 0),
    'catch': (1, 1),
    'catchProperty': (1, 1),
    'changeDispatch': (1, 2),
    'child': (2, 2),
    'childParentScope': (2, 2),
    'class': (1, 1),
    'clickSplit': (0, 0),
    'clipboard': (2, 2),
    'clock': (2, 2),
    'collectionDH': (2, 2),
    'collectionDesign': (3, 3),
    'collectionFields': (2, 2),
    'collectionRepeatDH': (2, 2),
    'colorTransform': (1, 1),
    'concat': (2, 2),
    'contains': (3, 3),
    'controller': (1, 1),
    'countdown': (2, 2),
    'dataRefDH': (2, 2),
    'directEvent': (2, 2),
    'dispatch': (2, 2),
    'dispatchDelayReset': (2, 2),
    'dragCursor': (0, 0),
    'draggable': (1, 3),
    'draggableWindow': (1, 1),
    'droppable': (3, 3),
    'entityDH': (2, 2),
    'event': (1, 2),
    'eventSequence': (2, 2),
    'fade': (4, 4),
    'file': (2, 2),
    'firstEntityDH': (2, 2),
    'generator': (2, 2),
    'generatorDH': (2, 2),
    'gui': (2, 2),
    'handleEventDH': (2, 2),
    'imeEnable': (1, 1),
    'indexOf': (3, 3),
    'inoutAction': (1, 2),
    'input': (0, 0),
    'instance': (2, 2),
    'keyboard': (2, 2),
    'lag': (0, 1),
    'mc': (2, 2),
    'menu': (0, 0),
    'pluralText': (2, 2),
    'popup': (0, 3),
    'popupNoCache': (0, 3),
    'primaryEntityDH': (3, 3),
    'repeat': (2, 2),
    'repeatCache': (7, 7),
    'repeatObject': (2, 2),
    'request': (2, 2),
    'resize': (2, 2),
    'restrictFeedback': (2, 2),
    'scopeHoldRepeat': (3, 3),
    'scopeTrace': (0, 0),
    'scrollController': (1, 1),
    'scrollControllerCentered': (1, 1),
    'serverTime': (1, 1),
    'soundOn': (1, 2),
    'stageSize': (0, 0),
    'style': (2, 2),
    'substitute': (3, 3),
    'sync': (3, 3),
    'timeFormat': (1, 1),
    'timeline': (3, 3),
    'timestampTween': (5, 5),
    'tooltip': (0, 0),
    'transition': (4, 4),
    'ubFocus': (1, 1),
    'vTileHack': (0, 0),
    'var': (1, 1),
    'visible': (1, 1),
    'watch': (2, 2),
    'watchDH': (3, 3),
}

NEEDED = {
    'menu': 2,
    'tooltip': 2,
}

MAX_READ = {
    'action': 4,
    'actionIsDisplay': 2,
    'appear': 6,
    'atlasText': 3,
    'catch': 4,
    'catchProperty': 4,
    'changeDispatch': 5,
    'class': 1,
    'clipboard': 2,
    'clock': 2,
    'collectionDH': 3,
    'collectionDesign': 4,
    'collectionFields': 3,
    'collectionRepeatDH': 5,
    'colorTransform': 1,
    'concat': 2,
    'contains': 5,
    'countdown': 6,
    'dataRefDH': 2,
    'directEvent': 4,
    'dispatch': 5,
    'dispatchDelayReset': 5,
    'draggable': 4,
    'draggableWindow': 3,
    'droppable': 4,
    'entityDH': 2,
    'eventSequence': 5,
    'fade': 7,
    'file': 2,
    'firstEntityDH': 3,
    'generator': 5,
    'generatorDH': 4,
    'gui': 4,
    'handleEventDH': 4,
    'imeEnable': 1,
    'indexOf': 3,
    'inoutAction': 4,
    'instance': 2,
    'keyboard': 4,
    'lag': 8,
    'mc': 4,
    'menu': 7,
    'pluralText': 2,
    'popup': 5,
    'popupNoCache': 5,
    'request': 4,
    'resize': 5,
    'restrictFeedback': 2,
    'scopeHoldRepeat': 5,
    'scrollController': 3,
    'scrollControllerCentered': 3,
    'serverTime': 2,
    'soundOn': 3,
    'substitute': 4,
    'timeFormat': 4,
    'timeline': 5,
    'timestampTween': 7,
    'tooltip': 7,
    'transition': 7,
    'var': 1,
    'watch': 2,
    'watchDH': 3,
}

PARTS = {
    'action': (
        (3, 'boolean', (), 'throw-lazy'),
    ),
    'appear': (
        (1, 'number', (), 'silent'),
        (2, 'number', (), 'silent'),
        (5, 'enum', ('0', '1', '2', '3'), 'silent'),
    ),
    'atlasText': (
        (2, 'number', (), 'unknown'),
    ),
    'changeDispatch': (
        (3, 'number', ('0', '1', '2'), 'silent'),
    ),
    'childParentScope': (
        (2, 'className', (), 'throw-init'),
    ),
    'class': (
        (0, 'cssClass', (), 'silent'),
    ),
    'collectionDH': (
        (0, 'number', (), 'silent'),
    ),
    'collectionRepeatDH': (
        (1, 'className', (), 'throw-lazy'),
        (4, 'boolean', (), 'none'),
    ),
    'contains': (
        (1, 'boolean', (), 'none'),
        (3, 'boolean', (), 'none'),
    ),
    'controller': (
        (0, 'controllerClass', (), 'throw-init'),
    ),
    'countdown': (
        (0, 'number', (), 'silent'),
    ),
    'directEvent': (
        (3, 'boolean', (), 'silent'),
    ),
    'dispatch': (
        (3, 'enum', ('0', '1', '2'), 'silent'),
        (4, 'number', (), 'throw-lazy'),
    ),
    'dispatchDelayReset': (
        (3, 'enum', ('0', '1', '2'), 'silent'),
        (4, 'number', (), 'throw-lazy'),
    ),
    'draggable': (
        (0, 'elementName', (), 'throw-init'),
    ),
    'draggableWindow': (
        (0, 'elementName', (), 'throw-init'),
    ),
    'droppable': (
        (0, 'elementName', (), 'throw-init'),
    ),
    'entityDH': (
        (1, 'number', (), 'silent'),
    ),
    'eventSequence': (
        (3, 'number', ('0', '1', '2'), 'silent'),
        (4, 'boolean', (), 'silent'),
    ),
    'fade': (
        (0, 'boolean', (), 'none'),
        (1, 'number', (), 'silent'),
        (5, 'enum', ('0', '1', '2', '3'), 'silent'),
        (6, 'number', (), 'silent'),
    ),
    'firstEntityDH': (
        (1, 'number', (), 'silent'),
    ),
    'generator': (
        (1, 'className', (), 'throw-lazy'),
    ),
    'generatorDH': (
        (1, 'className', (), 'throw-lazy'),
    ),
    'gui': (
        (3, 'boolean', (), 'throw-lazy'),
    ),
    'handleEventDH': (
        (3, 'enum', ('0', '1', '2'), 'silent'),
    ),
    'imeEnable': (
        (0, 'boolean', (), 'silent'),
    ),
    'instance': (
        (0, 'className', (), 'throw-init'),
    ),
    'keyboard': (
        (0, 'number', (), 'silent'),
        (3, 'number', (), 'silent'),
    ),
    'lag': (
        (2, 'boolean', (), 'silent'),
        (3, 'number', (), 'silent'),
        (5, 'number', (), 'silent'),
        (6, 'enum', ('0', '1', '2', 'UbScope.EVENT_DIRECTION_DOWN', 'UbScope.EVENT_DIRECTION_NONE', 'UbScope.EVENT_DIRECTION_UP'), 'silent'),
    ),
    'mc': (
        (0, 'className', (), 'unknown'),
    ),
    'menu': (
        (0, 'className', (), 'throw-lazy'),
        (6, 'boolean', (), 'none'),
    ),
    'pluralText': (
        (1, 'number', (), 'silent'),
    ),
    'popup': (
        (0, 'className', (), 'throw-lazy'),
        (2, 'number', (), 'none'),
    ),
    'popupNoCache': (
        (0, 'className', (), 'throw-lazy'),
        (2, 'number', (), 'none'),
    ),
    'primaryEntityDH': (
        (1, 'number', (), 'silent'),
    ),
    'repeat': (
        (1, 'className', (), 'throw-lazy'),
        (3, 'boolean', (), 'none'),
        (4, 'number', (), 'silent'),
    ),
    'repeatCache': (
        (1, 'className', (), 'throw-lazy'),
        (3, 'boolean', (), 'none'),
        (4, 'number', (), 'silent'),
        (5, 'number', (), 'none'),
        (6, 'number', (), 'throw-lazy'),
    ),
    'repeatObject': (
        (1, 'className', (), 'throw-lazy'),
        (3, 'boolean', (), 'none'),
        (4, 'number', (), 'silent'),
        (5, 'boolean', (), 'none'),
    ),
    'request': (
        (3, 'boolean', (), 'throw-lazy'),
    ),
    'resize': (
        (0, 'elementName', (), 'throw-init'),
        (1, 'enum', ('down', 'left', 'right', 'up'), 'silent'),
    ),
    'scopeHoldRepeat': (
        (1, 'className', (), 'throw-lazy'),
    ),
    'scrollController': (
        (1, 'number', (), 'silent'),
        (2, 'number', (), 'silent'),
    ),
    'scrollControllerCentered': (
        (1, 'number', (), 'silent'),
        (2, 'number', (), 'silent'),
    ),
    'serverTime': (
        (1, 'boolean', (), 'silent'),
    ),
    'style': (
        (0, 'styleProperty', (), 'throw-lazy'),
    ),
    'substitute': (
        (0, 'number', (), 'none'),
    ),
    'timeFormat': (
        (0, 'number', (), 'silent'),
        (3, 'boolean', (), 'silent'),
    ),
    'timestampTween': (
        (0, 'boolean', (), 'none'),
        (1, 'number', (), 'silent'),
        (2, 'number', (), 'silent'),
    ),
    'tooltip': (
        (0, 'className', (), 'throw-lazy'),
        (2, 'enum', (), 'throw-lazy'),
        (6, 'boolean', (), 'none'),
    ),
    'transition': (
        (0, 'boolean', (), 'none'),
        (1, 'number', (), 'silent'),
        (5, 'enum', ('0', '1', '2', '3'), 'silent'),
        (6, 'number', (), 'silent'),
    ),
    'ubFocus': (
        (0, 'number', (), 'silent'),
        (1, 'boolean', (), 'silent'),
        (2, 'boolean', (), 'silent'),
        (3, 'boolean', (), 'silent'),
    ),
    'visible': (
        (0, 'boolean', (), 'silent'),
    ),
}

ARITY_MODES = {
    'action': (
        '2 parts: data payload defaults to `{}` — dispatch fires unconditionally whenever the name (part1) evaluates non-null. CORRECTION: this is the MINORITY vanilla shape (19/607 uses per verb_worklist.json vanillaPartCounts {"2":19,"3":588}) — an earlier draft attributed the 588 count to this bucket, swapping it with 3-part.',
        "3 parts: data payload explicit; this is the DOMINANT real shape (588/607 uses). copyDataFromEvent stays null at this arity, so if part2's literal is a falsy non-object (`0`, `''`, `false`, or `null`) the `Boolean(_loc3_)` check at SFMEventBinding.as:68 fails and the ENTIRE dispatch is silently suppressed on every future firing. None of the sampled live 3-part `action` usages supply a falsy-literal part2 (all use literal object payloads, e.g. `{}`, `{points: currentPoints}`, `{rangeIdx:index, visible:true}`).",
        "4 parts (0/607 live uses — code-supported, unattested in this build): adds copyDataFromEvent. Same neutralizing interaction as `request`: when part3 evaluates truthy, `_loc3_ ||= {}` (SFMEventBinding.as:62) replaces a falsy part2 with a truthy `{}` BEFORE the line-68 suppression check, so dispatch is NOT suppressed in that case — suppression at arity 4 instead requires part3 itself to evaluate falsy. If part3 evaluates truthy AND part2's literal is a truthy NON-OBJECT primitive, the per-fire merge loop writes onto that primitive and throws #1056, conditional on the fired event's payload shape.",
    ),
    'actionIsDisplay': (
        '1 part: the length guard (line 34) is false, so eventData stays the default {} (line 33) and no watcher is created — the dispatched event always carries just {state: Boolean}.',
        '2+ parts: part[1] is both read once AND wrapped in a UbWatcher (lines 36-37), so its value can change dynamically afterward via onChange (lines 45-48) and is re-merged into every future dispatch; any part beyond index 1 is compiled but never read.',
    ),
    'appear': (
        '0 parts: throw-init - index0 read as a ctor argument on an empty vector.',
        '1-4 parts: init succeeds silently (only index0 is touched at init); the RangeError instead fires the first time the named event(s) actually dispatch (throw-lazy), which is the whole point of the binding, so this is not a rare corner case.',
        '5 parts: complete, minimal binding; easing defaults to Tween.EASE_LINEAR (0).',
        '6 parts: adds an explicit easing-id override.',
        '7+ parts: compiled but never read - silently inert.',
        'runtime-TYPE: a non-string literal at index0 is auto-coerced by addEventListener/scope.on - never throws, but a value that never matches any dispatched event leaves the tween permanently dormant.',
        'runtime-TYPE: non-numeric literals at index1/2/5 never throw - Tween.to clamps NaN duration/delay to 0, and assigning a non-numeric value to the `int` easing local silently becomes 0 (linear).',
        "runtime-TYPE: a non-object literal at index3/4 makes the immediate `for..in` pre-apply of index3 a silent no-op and forwards the raw literal into `Tween.to`'s native from/to parameters (ExternalInterface boundary, effect unverifiable from AS3).",
    ),
    'atlasText': (
        'Unique among these six verbs: NONE of the three reads are guarded. `UbBatchWatcher.createWatcher` (not the bounds-checked `tryToCreateOptionalWatcher` used by scrollController/scrollControllerCentered) is called directly with a raw indexed read for all three parts, in strict order 0,1,2.',
        "0, 1, or 2 parts: throws RangeError #1125 at init at exactly the first missing index — 0 parts fails on param1[0], 1 part fails on param1[1], 2 parts fails on param1[2]. Aborts the rest of the root element's subtree per fact 4.",
        '3 parts: the only valid/complete form; the class never reads a 4th part.',
        'Type coercion: `distance` (part 2) is delivered to `distanceChange(param1:Number)`, coercing any literal via AS3 Number conversion (non-numeric strings/objects become NaN) with no exception at that point.',
    ),
    'blockProperty': (
        "None beyond the fixed 2-part shape (matches vanilla's 3/3 uses, all exactly 2 parts). The only real 'mode switch' is which CONCRETE UbBlock subclass owns the element the binding is attached to — dispatch is by markup structure (the enclosing `<block className>`), not by any part's own value, paralleling `controller`'s dispatch-by-literal-className pattern except the dispatching literal lives OUTSIDE this binding's own value attribute. Both reads (index0 at UbBlockBinding.as:30 and index1 at UbBlockBinding.as:31) are unconditional and unguarded by any `param1.length` check anywhere in the file, which is what fixes minPartsInit/minPartsAll at exactly 2 — unlike every other verb in this batch, the source analysis for this verb never stated those two numbers explicitly even though the file itself makes them unambiguous.",
    ),
    'blurLayer': (
        'Value payload is entirely inert at every part count from 0 upward — init() calls `DisplayObjectUtils.markAsBlurLayer(param2)`, which only touches the DisplayObject target (param2/obj), never param1/expressions (UbBlurLayerBinding.as:17-21, re-confirmed by direct read: `override public function init(...) { super.init(...); DisplayObjectUtils.markAsBlurLayer(param2); }`). No literal, type, or count of parts changes behaviour in any way. 128 real vanilla uses, all with exactly 1 part, confirming the value is always supplied but never needed.',
    ),
    'blurMap': (
        'Identical inertness to blurLayer — init() calls `DisplayObjectUtils.markAsBlurMap(param2)` (UbBlurMapBinding.as:17-21, re-confirmed by direct read, structurally identical to UbBlurLayerBinding), never touching expressions. No literal, type, or count of parts has any effect. 138 real vanilla uses, all with exactly 1 part.',
    ),
    'catch': (
        "1 part: pure listener. onScopeEvent ALWAYS calls receive() with the raw payload of the scope event of this name (whatever some other dispatcher passes, possibly null/undefined); onBlockEvent (native flash event of the same name on the target obj) becomes a no-op since customDataExpression and eventScopeVarName are both unset (their String defaults '' are falsy).",
        '2 parts: adds customData shaping, gated at UbCatchEventBinding.as:34 — `_loc6_ = Boolean(_loc5_.requestedProps) || _loc5_.eval(param3) != null; this.customDataExpression = _loc6_ ? _loc5_ : null;` — a short-circuiting OR where eval() runs only if requestedProps is already falsy. For a genuinely dynamic (scope-referencing) part1 this is moot: requestedProps is non-empty so customData is always kept, live. For a pure LITERAL part1, the compiled requestedProps value is produced by an external, not-decompiled per-SWF expression module (loaded via UnboundLoader.as\'s onExpressionsLoaded -> processLoadedInfo -> `param1.doInitialize(this._expressions,this._requestedProperties)`, UnboundLoader.as:294-306,313-319) and cannot be read from this AS3 directly. The evidence strongly favors \'null/falsy for a literal, so eval()!=null decides\': that is the only reading under which this guard\'s second clause ever does anything (its evident purpose), and vanilla ships `<bind name="catch" value="\'ItemChooser.shipsSortSelected\'; null; \'shipSortChanged\'"/>` (markup_13187581.xml:13829) as an ordinary tech-tree-sort binding. Under the ALTERNATE reading (requestedProps truthy for a literal), customDataExpression would be kept and its eval() would be null on EVERY firing regardless of the real event payload, so onScopeEvent would call receive(null) deterministically every single time this event fires, and catch\'s own receive() has no null-guard (see otherStaticErrors) — `scope.copyDataFrom(null)` provably throws (UbScope.as:583,599) — meaning this common, presumably-shipping-and-working UI feature would crash on literally every use, not just intermittently. That deterministic-crash implication is strong (though not decompile-proof) evidence for the \'null reverts to raw payload\' reading; see openQuestions for how to settle it conclusively.',
        '3 parts: adds eventToDispatch - a scope event fired via scope.onEvent() once receive() finishes copying data. Evaluated exactly ONCE at init (UbCatchEventBinding.as:39), never re-evaluated, unlike part1 which IS re-evaluated fresh on every firing.',
        '4 parts: adds eventScopeVarName - a scope var that captures the raw flash Event object, but ONLY on the native block-event path (onBlockEvent); onScopeEvent (the UbScope-level path) never touches this field at all. Also evaluated once at init, never again.',
        "catchProperty (subclass) has identical init/arity grammar; only receive()'s effect on the resolved customData differs (see catchProperty spec).",
    ),
    'catchProperty': (
        "Identical arity grammar to 'catch' (UbCatchPropertyBinding extends UbCatchEventBinding and overrides ONLY receive() - UbCatchPropertyBinding.as:11-22). All init-time reads, guards, and part positions are exactly as documented for 'catch', including the same unresolved-from-decompile question about a literal-null part1's compiled requestedProps.",
        "Unlike 'catch', that ambiguity is INCONSEQUENTIAL for catchProperty specifically when part1 is a literal `null`: receive()'s own `if(param1==null){return;}` guard (UbCatchPropertyBinding.as:14-17) makes BOTH possible readings crash-free. If customDataExpression is dropped (the 'catch'-spec's favored reading), the raw scope-event payload flows into receive() and only no-ops if THAT happens to be null too; if customDataExpression is instead kept and always evaluates to null, receive(null) hits the same guard directly every time. The two readings differ only in whether a real, non-null event payload's fields ever get applied to `obj` — moot here since vanillaUses=0 leaves nothing to observe.",
        "The only behavioral difference from 'catch' otherwise is where the resolved customData/raw-payload object lands: 'catch' spreads it onto the SCOPE, 'catchProperty' assigns it directly as PROPERTIES on the target block instance `obj`, with its own null-guard 'catch' lacks.",
    ),
    'changeDispatch': (
        "2 parts: fires on every detected change of part0's value with an empty `{}` data object; direction defaults to `this.direction:int = 0` = UbScope.EVENT_DIRECTION_UP (bubbles to every ancestor scope) since part3 was never supplied - NOT EVENT_DIRECTION_NONE.",
        "3 parts: adds a data payload for the dispatched event, RE-EVALUATED fresh on every firing (reactive, unlike part3/part4's timing - see below).",
        "4 parts: adds an explicit direction, evaluated ONCE at init only (UbChangeDispatchBinding.as:29) and frozen for the binding's lifetime even if the expression is dynamic. Because the target field is `int`-typed, a Boolean or numeric-string literal does NOT behave like an out-of-range Number (see parts table) - it silently resolves to a real, specific direction.",
        "5 parts: adds a condition expression, STORED (not eval'd) at init but RE-EVALUATED fresh on every firing; the dispatch only proceeds when the watched value's NEW value LOOSELY equals (`==`, not `===`) this condition's freshly-evaluated result (UbChangeDispatchBinding.as:50).",
    ),
    'child': (
        "N=2 (no length>2 possible): dataModifier is permanently 1 for the binding's whole life — line46's data-object promotion check is unreachable. Whichever of expressions[0]/[1] is the discriminant/name pair, mode (array-of-one-name vs. 2-part map) is decided purely by whether expr[1]'s RUNTIME VALUE is an object.",
        "N>=3: line46 evaluates typeof(expr[1]) ONCE, synchronously, at bind time. If the CURRENT value is object-shaped, dataModifier is permanently promoted to 2 for the binding's life (a dataWatcher is attached to expr[1] to keep feeding the child scope) and the remaining name-slot logic shifts to start at index2; if not, dataModifier stays 1 and the name-slot logic starts at index1. This promotion is a one-time, init-time decision from expr[1]'s value at that instant — it is NOT re-evaluated if expr[1]'s type later changes.",
        "Given whatever dataModifier ends up being, line51 makes a SECOND, likewise one-time, type-driven decision: if expr[dataModifier] is object-shaped, the binding runs in swapChildrenByMap MODE for its whole life (exactly one object-literal part at index dataModifier acts as a `{key: blockClassName}` table, keyed by expressions[0]'s value); otherwise it runs in swapChildren ARRAY mode (every remaining part from dataModifier..N-1 is one candidate block class name, selected by `int(expressions[0].eval())`).",
        'Because fact #7 makes an object/array LITERAL statically knowable, a linter CAN determine array-vs-map mode with certainty whenever the relevant part(s) are literal object/array syntax or a literal primitive (string/number/bool/null) — the latter proves NON-object with certainty too.',
    ),
    'childParentScope': (
        'Mode is fixed ONCE at init from the runtime typeof of parts; later type changes never switch mode. \'object\' means typeof=="object": object literal, array literal, null, or any class instance. Strings/numbers/booleans/undefined/functions are NOT object.',
        'length == 2: dataModifier=1. part1 object -> MAP mode (part1 is the className map, no data). part1 non-object -> INDEX mode with a single class-name part (part1).',
        'length >= 3 and part1 NOT object: dataModifier stays 1 -> INDEX mode, class-name list = parts 1..n-1 (part1 is a class name, not data). UbChildBinding.as:46-51.',
        'length >= 3 and part1 object (incl. literal null or {}): part1 becomes DATA (dataWatcher, dataModifier=2), then part2 decides: part2 object -> MAP mode with map = part2, and parts 3..n-1 are NEVER READ (dead, silent); part2 non-object -> INDEX mode, class-name list = parts 2..n-1. UbChildBinding.as:46-49.',
        'INDEX mode: part0 is coerced to int by swapChildren(param1:int, param2:int) (ToInt32: \'2\'->2, \'abc\'->0, true->1, null/undefined->0, 1.7->1); child class = String(expressions[dataModifier+idx].eval) (UbChildBinding.as:101-102). Out of range above (but within the vector\'s total length) -> _loc3_=null -> className "null". Cache key = className+idx.',
        "MAP mode: part0 coerced to String by swapChildrenByMap(param1:String, param2:String) (0->'0', true->'true', null/undefined->null which indexes key 'null'); className = map[key] coerced to String (missing key/undefined -> null; number 5 -> '5'); cache key = the key. The map expression is re-evaluated only when part0 changes (no watcher on the map). UbChildBinding.as:106-110.",
        "The ParentScope override (UbChildParentScopeBinding.as:11-27) differs from plain 'child': (a) NO `if(!param1) return` guard, so null/''/missing-key class names go straight to factory.construct and throw instead of mounting nothing; (b) the child is constructed with the binding's OWN scope (childScope = scope), no new child scope; (c) data is never copied at mount, only via dataWatcher -> updateData, which copies into that same (parent) scope with setProperty per key (UbChildBinding.as:163-167 updateData -> copyDataFrom, UbScope.as:576-614), i.e. data keys overwrite same-named vars of the binding's own scope, visible to sibling bindings too.",
        "Construction happens INSIDE init (init -> swapChildren/swapChildrenByMap -> mountChild -> construct -> central.bindBlock), so a missing-plan throw or a throw in the child's own bindings aborts this binding's init.",
    ),
    'class': (
        'No arity modes by count: only part 0 is ever read (UbStyleClassBinding.as:23). Parts 1..n are compiled by bindingsFromString but never read by the binding - silently ignored.',
        'UbWatcher.update (UbWatcher.as:51-58) calls onChange when `newValue != oldValue`, OR (else-if) when `typeof newValue == "object"` even if unchanged -- and AS3\'s typeof(null) is "object", so a literal null is treated the SAME as any other object value for this purpose, not the same as undefined.',
        'Consequence at init: oldValue starts undefined (UbWatcher ctor param4 default). If part 0\'s initial value is a literal null, `null != undefined` is false (loose equality), but `typeof(null)=="object"` is true, so the else-if fires and onChange DOES run synchronously inside init\'s `watcher.update()` call (UbStyleClassBinding.as:24) -- the UbBlock cast in onChange executes AT INIT for a null value, not lazily. Only a genuinely undefined-valued initial expression (typeof "undefined", e.g. referencing an undeclared scope var) skips the callback at init, since neither branch fires.',
        'Value is coerced to String by addStyleClass(param1:String)/removeStyleClass(param1:String) (UbBlock.as:502/520): string -> used as the style class key; number -> e.g. "5"; boolean -> "true"/"false"; object literal -> "[object Object]"; array literal -> Array.toString join, so [\'$A\'] works as \'$A\' but [\'$A\',\'$B\'] becomes "$A,$B" (no match). null/undefined -> key null, i.e. styleClasses["null"] lookup, which effectively removes the previous class and adds nothing (on a UbBlock target).',
        'Transition X -> Y: removeStyleClass(X) (zeroes numeric computedStyle props the class set, deletes key) then addStyleClass(Y). Only one dynamic class is ever held per binding; a single string with several space-separated names is ONE key and matches nothing.',
    ),
    'clickSplit': (
        'Part count has NO effect on behavior whatsoever — init()/onEvent()/free() never touch `expressions` or `param1` at all. All 13 vanilla usages supply zero parts.',
    ),
    'clipboard': (
        "Arity is fixed at 2 semantically — no code path anywhere in the class reads index 2 or beyond; any extra parts exist in the fixed-length vector but are silently unused, mirroring `input`'s dead-tail-value pattern but only past index 1.",
    ),
    'clock': (
        '0 parts: throw-init (index0 read first).',
        '1 part: throw-init (index1 read second).',
        '2 parts: the entire grammar - the only one of the six verbs with a perfectly fixed, non-variadic arity.',
        '3+ parts: silently ignored - nothing in the class ever reads index2 or beyond.',
    ),
    'collectionDH': (
        '2 parts: childCollectionPath fixed to null forever — tracker always points at the ROOT collection for componentClassID.',
        '3 parts: index2 becomes a live-tracked reactive sub-path via UbWatcher/childPathUpdate — a different runtime mechanism, not just an added value.',
    ),
    'collectionDesign': (
        "Exactly 1 or 2 parts: regardless of part0's value or whether part1 is ever reached, `enable()` unconditionally constructs `expressionWatcher` from `expressions[2]` a few lines later in the SAME call (line 80) — this ALWAYS throws a RangeError at init with fewer than 3 parts; no execution path survives with only 2.",
        "Runtime TYPE of part0's resolved value gates whether part1 is ever read: `updateParentCollection()` treats a String as a scope-variable NAME to redirect through (`scope[param2]`, line 123), an Array as raw data auto-wrapped in `new CollectionImplement(...)` (line 127), and anything else that isn't already an IParentCollection is silently dropped (`if(!param2 || !(param2 is IParentCollection)) return;`, line 129). A literal of any OTHER type permanently disables the binding — re-evaluating the same wrong-typed constant on every retry never succeeds, nothing logged.",
        "CORRECTED (was mis-classified 'silent'): part3 (componentEvents), when supplied at all (arity>3), is committed via a strongly `Array`-typed field. `updateComponentFields()`'s `this.componentEvents = expressions[3].eval(scope);` (line 168) runs synchronously through the forced `componentEventsWatcher.update()` call at line 72, itself inside `enable()`/`init()`, gated only by `expressions.length>3` (arity), never by type. Any truthy literal for part3 that is not an actual Array (number, string, boolean, plain object literal) throws TypeError #1034 'Type Coercion failed' immediately at init — not silently.",
    ),
    'collectionFields': (
        "CORRECTED/EXPANDED (was: only a lazy length-mismatch hazard). fields (part1) and sortFunctions (part2) are BOTH strongly `Array`-typed fields (`private var fields:Array;` line 30, `private var sortFunctions:Array;` line 32). `updateFields()`'s `this.fields = param2;` (line 108) and `updateSortFunctions()`'s `this.sortFunctions = param2;` (line 118) run synchronously via the unconditional `fieldsWatcher.update()` (line 71) and the arity-only-gated `sortFunctionsWatcher.update()` (line 75), guarded ONLY by a falsy-check (`if(!param2) return;`, lines 104/114) — not by type. Any truthy literal for part1 (once arity is satisfied) or part2 (once supplied at all) that is not an actual Array throws TypeError #1034 immediately at init.",
        "Only once BOTH fields and sortFunctions are proper Array literals does the ORIGINAL length-mismatch hazard apply: sortFunctions is matched to fields POSITIONALLY (`_loc6_=this.fields.indexOf(param1); this.sortFunctions[_loc6_]`, lines 163-164). If sortFunctions is literal-array SHORTER than fields, the extra field(s) index past the end of a plain Array — reads as `undefined`, no throw — and that becomes `indexFunction` in the returned comparator (`sortFields()`, lines 169-188). The FIRST TIME that field's derived collection actually sorts with 2+ distinct values, `indexFunction(param1)` throws TypeError #1006 'value is not a function'.",
        "MISSED (confirmed, and GENERALIZED beyond the verifier's 'first segment' framing): `getFieldValue()` recurses per dot-segment via `param2[_loc3_]` (lines 190-198). Once ANY non-terminal path segment is absent on its parent — not only the first — the recursive call's argument becomes the primitive value `undefined` itself (not merely a missing property on a valid object). The VERY NEXT indexing of that value, `undefined[nextSegment]`, is a property access ON undefined (not a missing-property read per fact 5) and throws TypeError #1010 'a term is undefined and has no properties' (standard AS3 language behavior for this construct — not itself measured in-client for this binding). Only when the MISSING property is the path's LAST segment, reached through an otherwise-fully-present chain of real objects, does fact 5 apply and the lookup silently collapse to an undefined bucket instead.",
    ),
    'collectionRepeatDH': (
        "2 or 3 parts (collection[,itemClass][,path], no 4th 'externalData' part): never crashes, but `change()`/`changePath()`/`externalDataChange()` always call `tracker.update(this.externalData ? this.componentClassID : 0, this.path)` (DHCollectionRepeatBinding.as:154,166,172) with `this.externalData` falsy. RE-VERIFIED MECHANISM (corrects the original branch trace): on the strict-2-part case this first call is `tracker.update(0, null)`, and ChildCollectionTracker's OWN early-return guard fires first — `if(this.componentClass==param1 && this.path==param2){ return; }` (ChildCollectionTracker.as:54) — because the tracker's `componentClass:uint` defaults to 0 and `path:String` defaults to null, matching (0,null) exactly, so it returns before ever reaching the `componentClass!=param1` branch that calls `dataHub.getCollection()` (line 58-83). In the 3-part 'path present, no externalData' case, the guard at line 54 instead fails (`null==pathValue` is false) and execution falls into the sibling `else if(this.path != param2)` branch (line 85) — which reassigns `this.path` but never touches `this.root` (that only happens inside the `componentClass!=param1` branch), so `this.child` stays null and `getCollection()` still never runs. Two distinct branches, same silent-zero-items outcome; nothing is logged either way.",
        '4th part (externalData) type: only its TRUTHINESS gates whether the DataHub collection resolves at all (see above) — any truthy literal unblocks resolution. CORRECTED: `addItem()`\'s `scope.copyDataFrom(this.externalData)` (line 236) does NOT silently drop non-object literals. UbScope.copyDataFrom (UbScope.as:576-614) is exhaustive: `_loc2_=="Object"||_loc2_=="Dictionary"` spreads keys as named properties (584-590); `_loc2_=="Array"` stores under `\'dataArray\'` (591-594); `_loc2_=="int"||"uint"||"String"||"Number"||"Boolean"` stores under `\'$value\'` (595-598); a Vector also goes to `\'dataArray\'` (599-602); anything else falls through to `UbReflector.getPublicVariables` reflection (603-614). Every truthy literal type genuinely delivers data — a scalar just lands at `$value` instead of named properties, which is a usage detail for the itemClass view, not a defect.',
        "3rd part (path) literal string: a nonexistent path makes `Collection.getChildByPath` return null with no throw (Collection.as:199-219, confirmed: `_loc2_ = _loc2_.child(_loc3_[_loc4_]); if(!_loc2_){ break; }` then returns the broken chain's last non-null ancestor or null) — combined with a truthy externalData this again yields zero items silently, structurally identical in symptom to the missing-externalData case but reached by a different tracker-guard path (see mode 1).",
    ),
    'concat': (
        '0 vanilla uses in the shipped 13187581 markup - everything below is derived purely from the decompiled class body (which the decompiler itself partially garbled, see confidence note), not cross-checked against any real usage.',
        'Only indices 0 and 1 are ever read anywhere in the class; a 3rd+ part is never looked at.',
    ),
    'contains': (
        "3 parts: collection, per-`$item` boolean condition, result-field name. Every scan copies scope into an internal `_utilScope`, binds `$item` to each element, and writes a Boolean 'has a match' into the named field (evaluateHas, UbContainsBinding.as:126-146).",
        "4 parts: adds returnIndex - when its evaluated value is truthy, evaluateIndex() runs instead of evaluateHas(), writing the matching element's ARRAY INDEX (int, -1 if none) rather than a Boolean (UbContainsBinding.as:93-100,103-124).",
        '5 parts: part[4] REPLACES part[1] only as the WATCH TRIGGER used to decide when to re-run the whole scan; it does NOT become a second per-item test. evaluateHas/evaluateIndex always call `this.conditionExpression.eval(this._utilScope)` - i.e. part[1] - for every `$item`, regardless of whether part[4] exists. A mod author supplying part[4] expecting it to be the per-item predicate gets silently wrong (but non-crashing) semantics: their real per-item test is still part[1].',
    ),
    'controller': (
        "DISPATCH BY LITERAL CLASS NAME: total required parts = 1 (the class name itself, always read by UbControllerBinding before any controller-specific code runs) + however many indices THAT controller's own init() unconditionally reads. Table (all concrete UbController subclasses found via `extends UbController` plus a further sweep of every class extending one of those, 20 total, none deeper): UbModController, UIListController, UbBattleChatInputController, ControllerConvertExp, ControllerDivisionEntrance, ControllerLobby, ControllerResearchInset, ControllerShipInfo, ElasticBlockController, FakeCallbacksController, ControllerAutoScroll = min 1 (no extra unconditional reads). ScrollTrackerController = min 1 but is arity-agnostic: its init() override never calls super.init and never touches `param1` at all (a second, distinct instance of fact #4's 'override may skip super.init entirely') — confirmed at ScrollTrackerController.as:27-30, which only wires a scroll-event listener off `this._block`. UbInteractiveController (+ its subclass ControllerSystemLog, via the parent path) = min 1, optional up to 3. UbListBaseController (+ UbCarouselController) = min 1, optional up to 2/3. UbWebBrowserController = min 2. UbIMEWidgetInputController = min 7. ControllerArrowsPainter = min 10, optional up to 14. ControllerLoginQueue = min 3 (and skips super.init entirely).",
        "ControllerSystemLog silently discards any 'cursorOverName'/'scopeVarPrefix'-shaped extra parts a mod author might supply expecting UbInteractiveController's own optional index1/index2 behavior, because its override substitutes an empty vector before delegating to the parent — this is a silent no-op, not a throw.",
    ),
    'countdown': (
        '0 parts: throw-init - `new UbWatcher(param1[0],...)` (:61) reads index0 of an empty vector.',
        '1 part: throw-init - index0 succeeds but `param1[1].eval(param3)` (:62, fieldName) is out of range.',
        '2 parts: minimum valid countdown - handler defaults to onTimerTickSimple (plain HH:mm:ss via DateTime.timestampToHourMinSec), no completion event, no locale-form override, no raw-ms field.',
        "3 parts: activates format parsing and selects one of 4 tick handlers based on the literal's exact/prefix shape (see index2).",
        '4 parts: adds an on-countdown-finished event name, dispatched via scope.onEventUp (bubbles up).',
        '5 parts: adds a locale-form suffix, used by both the HIGHEST and HIGHESTDAYS handlers but interpreted differently by each (see index4 and otherStaticErrors).',
        '6 parts: adds a second scope field that receives the raw remaining milliseconds every tick; matches the vanilla maximum observed (1 sample) and the maximum meaningfully read.',
        '7+ parts: silently ignored.',
        "runtime-VALUE inside index2 independent of overall arity: a literal whose first 7 characters equal exactly 'HIGHEST' (case-SENSITIVE) is comma-split; every remaining segment must equal 'WITH_DAYS' case-INSENSITIVELY or UbCountdownBinding.init throws a hand-written Error synchronously during init - fully detectable from a literal string.",
    ),
    'dataRefDH': (
        'No arity modes: exactly parts 0 and 1 are read, both unconditionally at init (DHDataRefBinding.as:38-39). Parts 2+ are never read by the binding (silently ignored). No override of enable/disable; free() (:45-55) reads no part.',
        'Both parts are evaluated ONCE at init with no UbWatcher; a dynamic part is legal but never re-evaluated -- later changes of the expression are ignored.',
        "Both results are coerced to String by the typed fields (`private var scopePropertyName:String` :17, `private var pathToEntity:String` :19). A number/boolean/object/array literal is not a type error; it becomes its string form (e.g. {} -> '[object Object]', ['a','b'] -> 'a,b', 1.5 -> path segments ['1','5']).",
        "Part 1 path mode by its first dot-segment (DataHubWatcher.as:27-45): if segment 0 == '$dataRef' exactly, the chain is scope.$dataRef[seg1][seg2]... (the scope's own UbDataRef, filled by $dataRef.setRef); otherwise the chain is scope['$dataHubRefs'][seg0]['ref'][seg1]... i.e. seg0 names a data ref registered by getDataRef (part 0 of entityDH / firstEntityDH / primaryEntityDH / dataRefDH, or a watchDH event path), NOT a plain scope variable. Path '$dataRef' alone yields the UbDataRef object itself, not its ref.",
    ),
    'directEvent': (
        '2 parts: bare relay. Whenever the native flash event OR the scope-level event named by part0 fires, dispatch the native/Python action named by part1 (re-evaluated fresh each firing) with an empty `{}` payload - unless part1 evaluates to null, in which case the dispatch is permanently skipped (SFMEventBinding.as:68).',
        "3 parts: adds a data payload (part2), re-evaluated fresh each firing; the dispatch gate at line 68 is `Boolean(_loc3_)`, which is falsy for ANY of `null, 0, false, '', NaN` for part2's evaluated result, not only a literal `null` - so any of those literal shapes with no part3 present/truthy makes the whole dispatch silently inert forever (see otherStaticErrors).",
        "4 parts: adds copyDataFromEvent (part3) - when truthy at firing time, merges the raw native/scope event's own enumerable fields into the payload AND rescues a falsy part2 (not just null) back to `{}` via `_loc3_ ||= {}` (SFMEventBinding.as:62), un-doing the arity-3 silent-skip trap above.",
    ),
    'dispatch': (
        '1 part: THROWS AT INIT. `this.expressionEventName = param1[1];` (UbDispatchBinding.as:30) is unconditional and reads index 1 of a length-1 fixed vector -- out of range, which per fact 3 always throws (RangeError #1125) regardless of it being a store rather than a dereference. UbDispatchMultiDelayBinding.init reaches it via its own unconditional `super.init(...)`, so the throw happens before the watcher on part 0 is ever created.',
        '2 parts: listen for event part0, re-emit event part1 synchronously with args=null and direction=UbScope.EVENT_DIRECTION_UP (0) (UbDispatchBinding.as:90-91).',
        '3 parts: as 2, args = eval(part2) (any type, passed to listeners as param2 of onEventCustom).',
        '4 parts: as 3, direction = int(eval(part3)). UbScope.onEventCustom (UbScope.as:438-460): own-scope listeners are ALWAYS invoked first; then 0 = bubble to every $parent (bubbleEvent), 1 = broadcast to descendant scopes (broadcastEvent), 2 or ANY other int = no further propagation (switch has no default).',
        "5 parts: part 4 presence alone (length > 4, not its value) switches onEvent to the Timer path (UbDispatchMultiDelayBinding.as:54-61): every trigger creates `new Timer(eval(part4) * 1000, 1)` and fireEvent runs asynchronously on TIMER_COMPLETE; a delay value of 0 is still asynchronous. Multiple triggers stack independent timers (no reset; that is dispatchDelayReset's job). Delay is evaluated at trigger time, event name/args/direction at fire time.",
        '6+ parts: parts 5.. are never read (silently ignored).',
        'Part types: part0 and part1 results are coerced to String (`this.event` / `_loc1_:String`): null/undefined -> null (inactive), \'\' inactive, but false -> "false", 0 -> "0", {} -> "[object Object]" (all truthy, so a non-string literal silently listens for / dispatches a nonsense event name). part3 is coerced with int(): true->1, false/null/undefined/non-numeric string->0 (UP), 2.7->2. part4 is multiplied by 1000: null/false->0, true->1000, non-numeric string/object/undefined->NaN.',
        "Part-1 value falsy at fire time (null, '', undefined) -> fireEvent does nothing (UbDispatchBinding.as:88) - the documented way to make a dispatch conditional (vanilla `cond ? 'ev' : null`).",
        'Part-0 value is re-evaluated by a UbWatcher; when it changes, the old listener (scope.on + obj.addEventListener, weak) is removed and a new one installed (UbDispatchBinding.as:63-76).',
    ),
    'dispatchDelayReset': (
        '1 part: THROWS AT INIT. `this.expressionEventName = param1[1];` (UbDispatchBinding.as:30) is unconditional and reads index 1 of a length-1 fixed vector -- out of range, which per fact 3 always throws (RangeError #1125) regardless of the store-vs-dereference distinction. UbDispatchDelayResetBinding.init reaches this via its own unconditional `super.init(...)` call (line 28), after only the guarded, in-range part-4 check -- so the throw happens before any watcher on part 0 is created and before onEvent/fireEvent ever run.',
        '2 parts: fires part1 with args=null and direction EVENT_DIRECTION_UP (0) (:90-91) - bubbles to every ancestor scope.',
        '3 parts: args = eval(part2) at fire time.',
        '4 parts: direction = int(eval(part3)). No delay part -> onEvent calls fireEvent() synchronously (UbDispatchDelayResetBinding.as:62-65), i.e. behaves exactly like plain `dispatch` (no debounce).',
        '5 parts: debounce mode. Presence is decided by COUNT only (length > 4, :24), not by value: even a literal `null` 5th part yields a non-null IUbExpression, so the timer path runs with delay null*1000 = 0 (asynchronous fire), never the synchronous path. Every received event re-evaluates part4 and reset()+start()s the one-shot Timer (:70-74), so only the last event within the quiet window fires.',
        "5 parts, quirk: once a Timer exists, any later change of part 0's value (watcher -> onEventChange override :43-53) resets the timer and, if the new event name is truthy, starts it again - so part1 fires after the delay even though no new event was received.",
        '>=6 parts: extra parts are never read (silently ignored).',
        "Runtime type of part 0 / part 1: assigned to String-typed slots (`protected var event:String` UbDispatchBinding.as:18, `var _loc1_:String` :87), so a number/boolean/object is coerced to its string form (false -> 'false', 0 -> '0', {} -> '[object Object]'); null/undefined/'' disable listening (part0) or dispatch (part1) silently.",
        'Runtime type of part 3: int() coercion - non-numeric -> 0 (UP); ints other than 0/1 -> only own-scope listeners (same as NONE=2), per UbScope.onEventCustom switch :450-457.',
        "Runtime type of part 4: multiplied by 1000 (Number coercion): numeric string works, '' / null / false / [] -> 0, true -> 1000, [n] -> n*1000, non-numeric string / {} / multi-element array -> NaN.",
    ),
    'dragCursor': (
        'Part count has NO effect on behavior whatsoever — init()/free()/all callbacks never touch `expressions` or `param1`. Both vanilla usages supply zero parts.',
    ),
    'draggable': (
        "part0 literal null/'' (falsy): updateDrag(null) takes the `if(!param1)` branch — dragging is cleanly disabled for this instance, and parts 1/2 are NEVER read regardless of how many parts the vector actually has, so a 1-part binding whose value is deliberately falsy is completely safe.",
        "part0 truthy with length<3: registerDraggable IS called during init itself (watcher.update() at :61 runs updateDrag() synchronously the first time), so the FIRST time the user actually drags this item, the constructor() callback fires and unconditionally indexes expressions[1]/expressions[2] — RangeError #1125 out of a length<3 vector, thrown from inside the game's drag machinery mid-play, not at markup load.",
        "length<=3: lateBitmapCreation=false (default) — dragging becomes 'live' automatically once the mouse moves past a small distance threshold.",
        'length>=4: lateBitmapCreation=true — part3 names a scope event that must be dispatched to arm the drag; the automatic distance-threshold trigger in DragAndDropManager.onMouseMove is skipped (`&& !this.dragParams.lateBitmapCreation`).',
    ),
    'draggableWindow': (
        "length==1: isWindow stays false — dragging works but the window's position is never restored from or saved to a user pref; parts beyond 0 are never read.",
        'length==2: isWindow=true, but expressionUserPrefSubName is set to a literal null (see otherStaticErrors) — window position saving crashes the FIRST time the user releases a drag, or when the game clears a saved window position.',
        'length>=3: correct, fully-working persisted-position window drag (subName supplied).',
        'length>=4: extra parts are stored in the fixed vector but never read by any code path (dead).',
    ),
    'droppable': (
        'length<3: guaranteed RangeError #1125 at init on whichever of index1/index2 the vector is missing (index0 is checked first, so a 1-part vector fails on index1, a 2-part vector fails on index2).',
        "length==3: default scope property prefix 'droppableStatus' is used for scope.droppableStatus / droppableStatusState / droppableStatusData.",
        'length>=4: custom scope property-name prefix from part3 used instead of the default.',
        'part1/part2 literal shape: a scalar (string/number/etc.) is auto-wrapped into a 1-element Array by both onDroppableIdsChange/onDroppableFullChange; an Array literal is used as-is — either shape is accepted with no crash.',
    ),
    'event': (
        '1 part: init succeeds and the event listener is registered, but the first time the event fires onEvent dereferences expressions[1] (UbEventBinding.as:74), an out-of-range read on the fixed vector -- per fact 3 this throws (RangeError #1125), lazily, on first event delivery.',
        '2 parts: handler called with zero arguments (methodArguments = expressions.concat() then splice(0,2), :26-27).',
        "N>=3 parts: parts 2..N-1 are evaluated in order at event time and passed positionally to the handler via Function.apply (:90-96). The handler's arity must accept N-2 arguments (AVM2 ArgumentError #1063 on count mismatch for non-rest methods -- inference, not measured here).",
        "Part 1 runtime TYPE selects the dispatch mode: if it evaluates to a Function, it is called with this=scope (:75-79); otherwise it is used as a property NAME on the bindings target display object, obj[value] as Function, called with this=obj (:83-85). A LITERAL string therefore ALWAYS takes the obj-method path (a quoted 'resizeBrowser' looks up obj.resizeBrowser, never the scope variable resizeBrowser); only an unquoted expression can yield a scope Function.",
        "Part 0 is coerced to String by assignment to the String-typed field `event` (:14, :58): null/undefined -> null, '' -> '' (both falsy: no listener registered, binding inert); false -> 'false', 0 -> '0', an object -> '[object Object]' (truthy: a listener is registered on that literal name, which practically never fires).",
        'Part 0 is reactive: a UbWatcher re-evaluates it on dependency change and re-registers (removes old listener on scope+obj, adds new) (:51-64).',
        "The event name is registered on BOTH the scope event bus (scope.on, :61 -- receives onEventUp/onEventDown events such as dispatched ev* names) and as a native DisplayObject event on obj (obj.addEventListener(event,onEvent,false,0,true), :62 -- e.g. 'click', 'resize').",
    ),
    'eventSequence': (
        "2 parts: bare sequence. Whenever the listener event named by part1 fires, the next event name in part0's array is dispatched via scope.onEventCustom() with direction defaulting to UbScope.EVENT_DIRECTION_NONE=2 (DIFFERENT from changeDispatch's UP=0 default). No completion event; without part4, `finished` latches true after one full pass through part0's array and dispatchNextEvent early-returns on every subsequent listener firing (`if(this.finished) return;`) unless part0 or part1's watched value changes again and resets it.",
        '3 parts: adds a completion-event name (part2), fired once at the end of EVERY full pass through the array (not just the final one if repeating).',
        "4 parts: adds an explicit direction (part3), RE-EVALUATED fresh on every step via `int(expressions[3].eval(scope))` (reactive - unlike changeDispatch's frozen direction). Same int-coercion caveat as changeDispatch applies: a Boolean literal becomes a real direction rather than falling through.",
        '5 parts: adds a repeat flag (part4), re-evaluated at the end of each pass; truthy keeps `finished` from ever latching, allowing the sequence to restart automatically on the next listener firing without needing part0/part1 to change.',
    ),
    'fade': (
        '0-2 parts: throw-init (see reads for the exact index/line each count fails on).',
        "3 parts: always throws-init regardless of index2's literal value - if it evaluates non-null the next unconditional line reads the still-missing index3; if it evaluates null the same missing index3 is hit one statement earlier inside processPropsForOut.",
        '4 parts: minimum valid fade; customBack stays false; obj is shown/hidden using propsForOut.',
        "5 parts: merely being present forces customBack=true for every future out-transition, REGARDLESS of the 5th literal's value or type - even an explicit `null` disables propsForOut entirely.",
        '6 parts: adds an easing override.',
        '7 parts: adds a delay override; matches the vanilla maximum (62 samples).',
        '8+ parts: silently ignored.',
    ),
    'firstEntityDH': (
        '2 parts: one-shot resolution — the tracker is updated once at init; childCollectionPath stays null forever (tracks the ROOT collection).',
        '3 parts: index2 is instead wired through a UbWatcher (`childPathUpdate`), making the sub-path REACTIVE — a different runtime mechanism, not merely an added value.',
    ),
    'generator': (
        "generator ALWAYS calls `this.container.getFactory()` unconditionally at line 37, 3 statements into `init()`, BEFORE touching the parts vector at all — the only one of the six verbs where the host-type check is guaranteed to fire before any arity check, on every use, independent of the value attribute's content.",
        'Read order for the optional tail parts is non-monotonic in source: `param1.length>2` (part2, line43) is checked, then `param1.length>4` (part4/sortField, line47) is checked and read BEFORE `param1.length>3` (part3/sortArray, line51) is even checked. Not actually unsafe (Vector indices are contiguous: length=5 always implies index3 is in range whenever index4 is), but sortField (part4) can never be supplied without sortArray (part3) also present.',
        "CORRECTED (was 'no crash modes'): sortArray (part3) is a strongly `Array`-typed field (`private var _sortArray:Array=[];`, line 25). `addWatcher()` (lines 57-61) forces an immediate, synchronous `.update()` the moment the part3 watcher is constructed (guarded only by `length>3`, not type), invoking `onSortArrayChanged(undefined, evaluatedLiteral)` → `this._sortArray = param2;` (line 124). Any truthy literal that isn't an actual Array throws TypeError #1034 immediately at init.",
        "MISSED (confirmed): sortField (part4) is read ONCE via `this._sortField = param1[4].eval(param3);` (line 49) but, unlike sortArray, NO UbWatcher is ever created for it — it never re-evaluates if the scope expression's value later changes. Confirmed by grep: `onSortFieldChanged()` (lines 111-118) is defined but appears nowhere else in the file — dead code with exactly the shape a watcher callback would need, i.e. a genuine reactivity asymmetry/latent bug rather than intentional design.",
    ),
    'generatorDH': (
        "Unlike collectionRepeatDH, `change()`/`changePath()` here pass `this.componentClassID` to `tracker.update()` UNCONDITIONALLY (lines 164-174, confirmed: `this.tracker.update(this.componentClassID,this.path);`) — there is no externalData-gating bug. 2 parts (collection;itemRenderer) is genuinely enough for real items to generate; confirmed via ChildCollectionTracker trace: the first call is `tracker.update(componentClassID, null)`, and since the tracker's default `componentClass` is 0 while a real CC id is nonzero, the guard at ChildCollectionTracker.as:54 does NOT match, so `dataHub.getCollection()` runs and `onCollectionCreated()` fires — often synchronously inside init().",
        '3rd part (path): a wrong/nonexistent literal path makes `Collection.getChildByPath` return null with no throw (Collection.as:199-219) — `onCollectionRemoved` fires instead of `onCollectionCreated`, zero items render, silently.',
        "itemRenderer (part1) validity is only checked LAZILY relative to init: `generator.setItemRenderer(itemClass)` (line 138) just stores the name (UbCollectionGenerator.as:78-85, `setItemRenderer` only calls `clean()`). The actual `UbBlockFactory.construct()` call is deferred further still, to `UbCollectionGenerator.createItem()` (lines 195-237), itself reached only from `getItemByIndex()` which is called from LAYOUT code (`GeneratedLayout.as:158`, `CarouselVariableItemSizeLayout.as:213`) during the container's own measure/layout pass — very often the same frame as init, but a distinct pass, not literally inside init().",
    ),
    'gui': (
        '2 parts: data payload defaults to `{}` — dispatch fires whenever the name (part1) evaluates non-null. UNATTESTED in this build (0 live samples; the sole live `gui` usage is 3-part).',
        "3 parts — the only live sample in this build (`'click'; _method || _action || _request || _dispatch ? null : _name ? _name : null; {}`): part1 is dynamic (not a literal) and part2 is the literal `{}` (always truthy), so this real usage does not exercise the falsy-suppression trap. A falsy-non-object literal here would silently suppress every future dispatch (same rule as request/action at arity 3).",
        '4 parts: adds copyDataFromEvent (unattested — 0 live samples), with the same conditional #1056 merge-throw risk and the same line-62 neutralization of a falsy part2 as request/action when part3 evaluates truthy.',
    ),
    'handleEventDH': (
        '2 parts: dispatched custom scope event carries data=null (or `{$args:rest}` if the raw DataHub event had callback arguments) and direction=NONE(2).',
        "3 parts: adds a literal/expression object merged into the dispatched event's data, still NONE direction.",
        '4 parts: additionally supplies an explicit numeric direction, enabling UP(0)/DOWN(1) bubbling instead of the NONE default.',
        "If `obj[methodName]` (a truthy same-named property on the binding's own DisplayObject) is non-null at event time, parts 2 and 3 are completely ignored — the found value is invoked directly (`.apply`) with the DataHub event's own raw arguments instead of the scope-event path (DHHandleEventBinding.as:101-104).",
    ),
    'indexOf': (
        'Exactly 3 parts, no optional/variadic behavior anywhere in the class - matches all 3 vanilla samples (all part-count 3).',
        "part[1] (targetScopeName) is captured ONCE at init, exactly like `watch`'s name part - never re-evaluated afterward, so a dynamic (non-literal) part[1] is frozen at whatever it evaluates to the very first time.",
    ),
    'inoutAction': (
        "1 part: safe ONLY through init() itself, and only if the target DisplayObject is NOT already on the stage when this binding's init() runs (order/timing-dependent on where the enclosing element sits in UbCentral.bindBlockIteratively's construction, e.g. a dynamically-inserted list item vs. one present from the start). Effectively always crashes eventually - see otherStaticErrors.",
        '2 parts: minimum SAFE arity for ordinary use - dispatches an \'enter\' action on ADDED_TO_STAGE and an \'exit\' action on REMOVED_FROM_STAGE (and possibly from free()), both with empty `{}` data. Note: a literal `null` for either action name is NOT a silent no-op here (see parts table) - it dispatches the literal action name "null".',
        '3 parts: adds data for the enter action, evaluated fresh every ADDED_TO_STAGE firing.',
        '4 parts: adds data for the exit action too, evaluated fresh every REMOVED_FROM_STAGE firing (and at free() if applicable).',
    ),
    'input': (
        "Part count and part type are completely irrelevant: no method anywhere in InputMappingBinding or its UbBinding base ever indexes into `expressions` (confirmed by re-reading the full class, InputMappingBinding.as:1-81 — init/onAdded/onRemoved/registerInput/unregisterInput/free never reference `expressions`). Behavior (register the block's DisplayObjectContainer parent + block via InputMapping.registerGuiButtonsByScope on add-to-stage, InputMapping.unregisterGuiButtonByScope on remove) is identical for 0, 1, or N parts.",
    ),
    'instance': (
        'Exactly indices 0 and 1 are ever read (UbInstanceBinding.as:36-37); no other method (onNameChanged/onDataChanged/cleanUpChild/free) indexes expressions. Parts 2+ are compiled but never read - silently ignored.',
        'Part 0 is stored into `private var elementName:String` (:23,87): String coercion applies (undefined/null->null, 5->"5", true/false->"true"/"false", {}->"[object Object]", []->"", [\'A\']->"A"). Gated by Boolean() at :88/:97; used verbatim (no trim, case-sensitive) as the construct() className.',
        "Part 1's runtime type selects UbScope.copyDataFrom's dispatch (UbScope.as:576-614): Object/Dictionary -> each key copied; Array -> 'dataArray'; int/uint/String/Number/Boolean -> '$value'; anything else -> Object(x).isVector()/UbReflector fallback.",
        "createChild only runs when Boolean(elementName) && Boolean(elementData) (:88,97) - both gated by Boolean(), so unlike 'repeat's array elements, part1 is never handed to copyDataFrom while falsy. Falsy data (0,'',false,null,NaN,undefined) means no child ever, or tears one down (:110); {} and [] are truthy and do construct.",
        "Timing: init calls nameWatcher.update() first (:38, elementData still undefined, so onNameChanged never constructs on that first call), then dataWatcher.update() (:39): onDataChanged constructs AT INIT when both parts are already-truthy literals. A part that is dynamic and only turns truthy later constructs lazily from that watcher's callback.",
    ),
    'keyboard': (
        '2 parts (2/5 live uses): key toggles the scope event only; no down-state scope field written, no debounce (clickInterval=0).',
        '3 parts (0/5 live uses — code-valid, unattested): adds isButtonDownScopeFieldName — a scope property mirroring live is-key-down state. See otherStaticErrors for what happens if this literal collides with a real UbScope member name.',
        '4 parts (3/5 live uses): adds clickInterval — minimum ms between successive dispatches of the same key-down.',
    ),
    'lag': (
        "0 or 1 part: init never throws -- every index init touches (2,3,4,5,7) is guarded by `expressions.length > N`, and indices 0/1/6 are never touched by init at all. The binding arms a listener on the default trigger event ('addedToStage') using every other default. Failure is deferred to the lazy onEnterFrame path, and it is NOT the very next frame: on the first ENTER_FRAME tick after the trigger fires, deltas.length==1, which is below the default framesCount=3, so the fps-stabilization clause (`fps>=fpsStabilization && deltas.length>=framesCount`) is false, and elapsed time is far under the default maxWaitTime=1s, so the time clause is also false -- the trip condition at onEnterFrame:127 cannot fire on tick 1. The earliest a 0/1-part binding can throw the unguarded `expressions[0]` read at onEnterFrame:129 is the 3rd ENTER_FRAME tick after the trigger (once deltas.length reaches framesCount, assuming the framerate is already stable enough), or later -- up to roughly maxWaitTime seconds of ticks -- if it never stabilizes.",
        '2 parts: adds an event-data payload (part1) passed with the eventually-dispatched event.',
        '3 parts: adds shouldRepeat -- re-arms the check after it fires once.',
        '4 parts: adds maxWaitTime (seconds) override for the timeout branch.',
        "5 parts: overrides the *trigger* event (part4) that starts the frame-rate/timeout check, replacing 'addedToStage'.",
        '6 parts: overrides framesCount, the number of consecutive stable frames required for the FPS-stabilization branch.',
        "7 parts: adds the UbScope event-direction (UP/DOWN/NONE) used when dispatching part0's event.",
        "8 parts: overrides endCheckEvent (default 'startHide'), an event that cancels the pending check if it fires first -- an explicit literal `null` here (rather than omitting the part) is NOT equivalent to omission: it sets endCheckEvent to null, disabling the cancel feature entirely (see otherStaticErrors); this exact pattern appears in sampled vanilla markup.",
    ),
    'mc': (
        '0 or 1 parts: both throw RangeError #1125 at init — on `expressions[1]` (events), not `expressions[0]` (className), because the code reads index 1 first. So even a syntactically-plausible single-part `<bind name="mc" value="\'SomeClass\'"/>` throws immediately, aborting the rest of the root element\'s subtree (fact 4).',
        'Exactly 2 parts: className + events; `properties` stays at its class-field default `[]` (MovieClipBinding.as:22) and `initValues` stays at its default `{}` (line 24) — the `for each`/`for-in` loops over these are simply no-ops, not errors.',
        '3 parts: adds `properties`; `initValues` still defaults to `{}`.',
        '4 parts (the only form seen in vanilla, 1 usage): all four parts populated — each `initValues` key becomes a direct property WRITE on the newly-constructed instance.',
        '5+ parts: accepted by the parser but never read anywhere in this class — silently wasted, no error.',
        'className (part 0) resolution has three outcomes independent of arity: (a) the string is not a registered definition → silent no-op, nothing is ever created; (b) it resolves to an existing definition that is not constructible or does not extend DisplayObject → throw (init, for a literal, since `.update()` forces the first eval synchronously in init; lazy, for a dynamic expression that only later evaluates to the bad value); (c) it resolves to a good DisplayObject subclass → proceeds to add the child (subject to the initValues/target-type hazards below).',
    ),
    'menu': (
        "UbContextMenuBinding.init (UbContextMenuBinding.as:16-21, re-confirmed) sets `tipBehaviour = mapBehavioursIdsByName['contextMenu']` (id 10) and `isExternalTipBehaviourSet = true` BEFORE calling super.init — so the inherited guard at UbTooltipBinding.as:87 (`!this.isExternalTipBehaviourSet`) is permanently false for every 'menu' binding: part[2] is NEVER evaluated, regardless of part count or content.",
        '0-1 parts: same as tooltip — the watcher never gets created, the menu never opens, silent no-op.',
        "2 parts (minimum functional shape): element className + gating/data value; behaviour fixed at 'contextMenu'. super.init()'s synchronous `watcher.update()` (UbTooltipBinding.as:79) still applies: real 'menu' binds more often gate part[1] behind a runtime ternary than plain 'tooltip' does (e.g. `isSuspended ? 0 : 1`-style, or `(false) ? {...} : null`), so the eager path is less dominant here, but still occurs whenever part[1] is an unconditional literal (e.g. 'TableRowMenu').",
        "3+ parts: part[2] is present but completely inert; vanilla always fills it with `null` as an unused positional placeholder (6 of 22 real 'menu' binds use exactly 3 parts, all following this convention).",
        "5+/6+/7+ parts: identical to tooltip (tipVisibleScopeName / tipStateScopeName / auto-open-timer overrides), including the null-vs-omit trap for part[4] — CONFIRMED live in vanilla's own 'ChannelContextMenu' bind, the exact real-world sample of this defect (see otherStaticErrors).",
    ),
    'pluralText': (
        'Exactly 2 parts is the only meaningful arity: only expressions[0]/[1] are ever read (init:28-29); any part at index>=2 is compiled into the fixed-length vector but never evaluated (dead literal, no crash).',
        'The evaluated value of part0, coerced to String by onTextChange\'s String-typed parameter, selects the render mode: if `this.text.indexOf("IDS")==0` (first 3 chars exactly \'IDS\'), text is rendered via native `Translator.translatePlural(text,count)` using part1\'s count; otherwise `update()` writes part0 verbatim to `obj["text"]` and returns early -- part1\'s cached count is never consulted for that render (but is still read once at init, so a missing part1 still throws).',
    ),
    'popup': (
        '0-2 parts: FATAL under ordinary use. enable() succeeds at init (the index-4 guard covers 0-4 parts), but the default reason.mouseOver listener is always wired to obj; the instant the pointer enters obj, show() (UbPopupBinding.as:517) evaluates expressions[PRIORITY_ORDER] (index 2) — RangeError #1125, thrown synchronously inside the mouse-over handler, before any popup content is built.',
        '3 parts (minimum safe & functional shape): element className, non-null data, numeric priority. Default MousePopupSpacingComponent and DEFAULT_EVENTS_INTERACTIONS (mouseOver→show, mouseOut/removedFromStage/popup_opened→hide) are used.',
        "4 parts: adds a custom spacing-component descriptor `{type, params}` replacing the default MousePopupSpacingComponent. CORRECTED constraint: `type` must name a class that both resolves via `getDefinitionByName` AND extends `PopupSpacingComponentBase` (re-confirmed: `spacingComponent:PopupSpacingComponentBase` at UbPopupBinding.as:190) with a compatible 4-arg constructor `(dataHub:DataHub, tooltipBlock:UbBlock, obj:DisplayObject, params:Object)` — the 3 vanilla classes all follow exactly this shape, forwarding a 5th 'defaults' object to the base class internally (confirmed: MousePopupSpacingComponent's 4-param constructor calls `super(param1,param2,param3,param4,DEFAULT_PARAMS)` against PopupSpacingComponentBase's 5-param constructor). A resolvable-but-unrelated class throws TypeError #1034 ('Type Coercion failed') on assignment; a resolvable-and-related class with mismatched constructor arity throws ArgumentError #1063 on `new`. 'any resolvable+constructible class' is NOT a sufficient description.",
        '5+ parts: adds a custom events-interactions object `{reason:{...}, reasonScope:{...}}` replacing DEFAULT_EVENTS_INTERACTIONS wholesale (not merged with it).',
        '6+ parts: no code reads index 5 or beyond — silently inert.',
    ),
    'popupNoCache': (
        "Identical to 'popup' in every part-count and literal-constraint respect (UbPopupNoCacheBinding.as is a re-confirmed 14-line subclass — `useCache=false; super();` — that only sets useCache=false before delegating entirely to UbPopupBinding).",
        "The only behavioural difference from 'popup': constructElement()'s cache-hit branch always takes the null/no-cache path, and cashElement() always frees the block/scope instead of pooling them — this changes reuse/perf, not arity or literal validity.",
        "0-2 parts: same fatal RangeError on first hover as 'popup' (show() reading index 2). 3 parts: minimum safe shape. 4 parts: same CORRECTED spacing-component constraint as 'popup' (type must resolve AND extend PopupSpacingComponentBase AND match the 4-arg constructor shape). 5+ parts: same events-interactions customization as 'popup', including the reflective-action-dispatch and non-null-primitive traps.",
    ),
    'primaryEntityDH': (
        "3 parts: updateFnName stays undefined; the `Boolean(this.updateFnName)` check in setEntityWatcher is false, so only the Entity's own evAdded/evRemoved drive onEntityUpdated — the extra per-field Invoker wiring is skipped entirely.",
        "4 parts: updateFnName is looked up as a property name on the entity's OWN COMPONENT (`entity.getComponent(componentClassId)[updateFnName]`), coerced into an Invoker-typed local; if that coercion succeeds and yields non-null, `.addCallback`/`.removeCallback` are called on it.",
    ),
    'repeat': (
        '1 part or 0 parts: throws at init at UbRepeatBinding.as:42 (`itemClass = param1[1].eval(param3);`) before part 0 is ever touched or any watcher exists.',
        '2 parts: data/count (part0) + item className (part1); no external data, forward order, no add-delay.',
        '3 parts: + external data expression (part2), evaluated once at init (:46); a watcher is created ONLY if it is truthy at init (:46-48), so a value that starts falsy and only later becomes truthy is never re-pushed into scopes that already exist - it is still copied into NEW item scopes on every subsequent change() (:139, :150-153).',
        "4 parts: + useBackDirection (part3), Boolean-coerced and watched; a later flip swaps ALL of the container's children (revertDirection, :239-254), not only the repeated ones.",
        '5 parts: + per-child add delay in seconds (part4), Number-coerced, evaluated once at init and never watched; NaN->0; >0 switches doAddAllChildren to a staggered setTimeout chain (doAddChild, :169-176/197-219).',
        '>=6 parts: parts[5..] are never read - silently ignored.',
        "Type of part0's runtime value (change(), :108-111): Array or Vector (isVector()) -> one item per element, each item scope gets copyDataFrom(element)+$dataRef.setRef(element); any other value -> count = max(0,int(value)) (numeric string parsed, true->1, plain object/non-numeric string->0); falsy (null/0/''/false/NaN) -> [] -> zero items, silently.",
        "Because part0's watcher.update() runs synchronously inside init (UbRepeatBindingBase.init :33-34, called from the end of this class's own init at :63) BEFORE any scope-var dependency can mark it dirty, a purely LITERAL part0 is processed by change() exactly ONCE, ever - at init - and always through the 'new scopes' branch (oldLen starts at 0, so the 'existing scopes' branch at :127-138 is unreachable for a literal; it only matters for a genuinely dynamic part0 that changes after init).",
    ),
    'repeatCache': (
        'No effective arity modes below 7: the override init (UbRepeatBindingCache.as:45-68) does NOT call super.init anywhere in its body, and unconditionally dereferences [1],[4],[5],[6], so fewer than 7 parts always throws at init before any timer starts. Every length guard downstream (furtherInit :72/:79, UbRepeatBinding.init :43/:50/:54) is therefore always satisfied once init itself has survived. Parts at index 7+ are never read - silently ignored.',
        "Part 4 has two readings: it is stackLen (the pre-built pool size, :56), and UbRepeatBinding.init (reached later via furtherInit's super.init chain) also reads the SAME index as childrenDelay seconds (UbRepeatBinding.as:56-57). The second reading is inert: only UbRepeatBinding.change uses childrenDelay, and this class overrides change() and always calls doAddAllChildren() (:257), ignoring childrenDelay entirely.",
        'Timing: after initDelay ms the pool is built one block per creationDelay tick (itemsPerFrame=1, :92-99). Only once currentStackLen==stackLen does furtherInit run (:100-104), which is the FIRST point part0 is watched and any items are shown - so stackLen==0 makes furtherInit run on the very first creation tick; a negative stackLen makes it never run (see otherStaticErrors).',
        "furtherInit's own super.init(...) call (:87) re-runs the entire UbRepeatBinding.init/UbRepeatBindingBase.init chain, so parts [1]-[4] are each evaluated a SECOND time there (with the same guards, same values, harmless), and part0's watcher (and its first change() call) is created only at this point - always from inside a Timer callback, i.e. always a LAZY trigger, never truly synchronous with the outer bind() call.",
        "Part 0 runtime type in change() (identical dispatch to plain 'repeat', :174-177): falsy -> []; Array or Vector -> one item per element; any other value -> int(Math.max(0,int(v))) items.",
    ),
    'repeatObject': (
        "1 part or 0 parts: throws at init at UbRepeatBinding.as:42 (`param1[1].eval(param3)`), reached via this class's init calling `super.init(...)` before part0 or part5 (isFilterEnabled) are ever meaningfully used.",
        '2 parts: data (part0) + item className (part1); no external data, forward order, no delay, no key filter.',
        "3 parts: + external data expression (part2), evaluated at init; a watcher is created ONLY if truthy at init (UbRepeatBinding.as:46-49), same rule as plain 'repeat'.",
        '4 parts: + useBackDirection (part3), watched.',
        '5 parts: + per-child add delay in seconds (part4), evaluated once at init, not watched.',
        "6 parts: + isFilterEnabled (part5), Boolean-coerced, evaluated ONCE at init (UBRepeatObjectBinding.as:75-78) BEFORE parts 0-4 are touched (this class's init runs before calling super.init). Only affects object (non-Array) data: drops keys whose value is a function or a UbScope (isSimpleType, :58-71).",
        '>6 parts: extra parts compiled but never read - silently ignored.',
        "Runtime type of part0 (convertValue, :18-56, then inherited UbRepeatBinding.change): falsy -> [] -> zero items; Array or Vector -> PASSED THROUGH UNCHANGED (`if(param1 is Array || Boolean(Object(param1).isVector())) { return param1; }`), one item per element exactly like plain 'repeat' including its guard gaps; any other truthy value -> for..in over its enumerable keys (sorted), one item per key with {$index_key_name,$index_key_value} wrapper data. A Number/Boolean/non-empty-String part0 therefore yields ZERO items (for..in over a primitive enumerates nothing), unlike 'repeat' where a number is a count.",
        "This class's own change() override (:82-85) always forwards into the inherited UbRepeatBinding.change() (super.change), so its timing is identical to plain 'repeat': the first call runs synchronously inside the outer init() call for a literal part0 (no timers are involved, unlike repeatCache).",
    ),
    'request': (
        '2 parts (14/108 live uses): data payload defaults to `{}` (SFMEventBinding.as:59, `this.exprEventData != null ? ... : {}` — always truthy) — dispatch fires unconditionally whenever the name (part1) evaluates non-null.',
        "3 parts (94/108 live uses — the dominant real shape): data payload explicit. copyDataFromEvent stays null at this arity (never assigned, param1.length>3 is false), so `Boolean(this.copyDataFromEvent)` at line 60 is false and the `_loc3_ ||= {}` neutralizer at line 62 never runs: if part2's literal is a falsy non-object (`0`, `''`, `false`, or `null`) the line-68 `Boolean(_loc3_)` check fails and the ENTIRE dispatch is silently suppressed on every future firing. None of `request`'s 94 live 3-part samples actually supply a falsy-literal part2 (all use literal object payloads like `{}` or `{ _rulesType: 'arc_event' }`) — the trap is code-derived, not attested by any real usage in this build.",
        "4 parts (0/108 live uses — code-supported, unattested in this build): adds copyDataFromEvent, re-evaluated every fire (line 60: `this.copyDataFromEvent.eval(scope)`). CORRECTION to an earlier draft that stated the arity-3 suppression rule unconditionally: at arity 4, when part3 evaluates truthy, `_loc3_ ||= {}` (line 62) replaces ANY falsy part2 result with a fresh truthy `{}` BEFORE the line-68 suppression check, so a falsy part2 literal no longer suppresses dispatch at this arity — dispatch still fires (with just the merged event data, or an empty `{}` if the fired event has no own enumerable properties). Suppression at arity 4 instead requires part3 itself to evaluate falsy. Separately: if part3 evaluates truthy AND part2's literal is a truthy NON-OBJECT primitive (nonzero number / non-empty string / true), the per-fire merge loop (lines 63-66, `for(_loc4_ in param1){ _loc3_[_loc4_]=param1[_loc4_]; }`) writes properties onto that primitive (a sealed type) and throws #1056 — but only if the fired event object itself carries >=1 own enumerable property (plain-Object custom scope events do; sealed native events like MouseEvent do not, so the loop body never executes for those).",
    ),
    'resize': (
        "length<2: RangeError #1125 in the base class's init (index0 or index1 out of range) before WowsResizeBinding's own subclass logic ever runs.",
        'length==2: pure interactive resize; nothing is ever persisted to/restored from ModPrefs.',
        'length==3: width persisted (userPrefsWidthName) and restored at init if present; height is still live-resizable (if direction includes up/down) but never persisted.',
        'length==4: both width and height persisted under flat (non-nested) pref keys.',
        'length>=5: width/height persisted nested under a shared sub-key (userPrefsSubName), e.g. per-channel chat sizing.',
        "A literal null for part2 or part3 explicitly SKIPS persistence for that one axis (Boolean(null) is false, guards in setDragStateToFalse/init both short-circuit) while still allowing the OTHER axis's pref name to be supplied at a later index — a real, deliberate vanilla idiom, not a bug.",
    ),
    'scopeHoldRepeat': (
        "idKey (part2) is read UNCONDITIONALLY and FIRST — earlier than itemClass (part1) and the collection expression (part0), which are only read afterward via `super.init()` (called last, line 46). A 2-part scopeHoldRepeat binding throws immediately on the missing idKey, never reaching the itemClass/collection reads — an easy mistake by analogy with the other repeat-family verbs' 2-part minimum.",
        "The idKey VALUE's uniqueness across items (not its literal-ness — idKey is always a property NAME string) determines correctness: `getScopeById`/`getElementById` (lines 109-128) key Dictionaries by each item's `[idKey]` value; two items sharing that value collapse into one child. Data-dependent, not literal-checkable from the idKey string alone.",
        "CORRECTED (was 'no crash modes'): sortArray (part4) is a strongly `Array`-typed field (`private var sortArray:Array;`, line 24). The direct, unconditional-once-`length>4`-passes assignment `this.sortArray = param1[4].eval(param3);` (line 43) throws TypeError #1034 immediately at init — before even `super.init()` runs (line 46) — for any truthy literal that isn't an actual Array. The watcher callback `onSortArrayChange(param1:Array,param2:Array)` (line 79) is ALSO Array-typed on both parameters, so a later scope-driven re-evaluation to a non-Array value throws at the callback-invocation boundary too (only reachable via a dynamic, non-literal part4 expression, since a literal never changes value).",
    ),
    'scrollController': (
        "0 parts: throws RangeError #1125 at init on the unguarded `expressions[0]` read (line 44), aborting the rest of the root element's subtree (fact 4).",
        "Exactly 1 part (prefix only): `tryToCreateOptionalWatcher` skips both index 1 and 2 (length 1 is not > 1 or > 2), so `this.watcher` ends up with ZERO wrapped watchers. Traced through UbBatchWatcher/UbWatcherWrapper/UbExpressionWatcher: with no watchers, `forceUpdate()`'s for-each loop (UbBatchWatcher.as:49-56) runs zero iterations, so the batch callback `update()` (which calls `addListeners()`) never fires at init. Result: totally inert — `<prefix>CanScrollRight/CanScrollLeft/ScrollEnabled` are never written to scope and none of the `<prefix>ScrollToIndex/ScrollNext/ScrollPrev/ScrollToTop` scope-event handlers are ever wired. No crash, regardless of target type.",
        "2 or 3 parts: at least one optional watcher exists. Traced mechanism confirms this IS synchronous: creating a watcher immediately OR's its bit into `UbBatchWatcher.dirtyWrappersInTickCount` (UbBatchWatcher.as:37-38, via the private `onChanged` at 59-62); `forceUpdate()` (line 47) then calls each `UbWatcherWrapper.forceUpdate()`->`update()` (UbWatcherWrapper.as:51-61), which runs `UbExpressionWatcher.update()` (evaluates the expression, invokes the onItemSizeChange/onRowsCountChange callback), then fires `onWrapperMadeCall` (sets batch.dirty=true) and `onAfterWrapperUpdate` (clears the bit; once all outstanding bits clear, calls the batch's private `update()`, which invokes `UBScrollBinding.update()` synchronously) — so `update()`->`addListeners()` genuinely runs inside `init()`, not merely 'as if'. This is also the point where a wrong (non-UbBlock) target throws (see otherStaticErrors).",
        'With rowsCount omitted (2 parts): rowsCount stays at its int field default of 0. The next time a `<prefix>ScrollNext/ScrollPrev/ScrollToIndex` scope event fires, `Math.floor(this.currentIndex / this.rowsCount)` (line 108) and `Math.floor(-scrollPosition * this.rowsCount / this.itemSize)` (line 140) divide by zero. AS3 numeric division by zero never throws — it yields NaN/Infinity, which `int`-coercion elsewhere collapses to 0 — so scrolling silently breaks instead of crashing.',
        "Type coercion: itemSize/rowsCount are delivered through Function parameters typed `int` (`onItemSizeChange(param1:int)`/`onRowsCountChange(param1:int)`), so a non-numeric literal (unparsable string, object, array, boolean) coerces via AS3's ToInt32 rules to 0 with no exception — the same effect as omitting the part.",
    ),
    'scrollControllerCentered': (
        '0 parts: throws RangeError #1125 at init on the inherited unguarded `expressions[0]` read, aborting the subtree (fact 4) — identical to scrollController.',
        'Exactly 1 part: identical to scrollController — zero optional watchers get created, so the (overridden) batch callback `update()` never fires at init; the widget is totally inert (no listeners wired, `currentCenterIndex`/CanScroll*/etc. never set), regardless of target type.',
        '2 or 3 parts (all 4 vanilla uses are exactly 2 parts): at least one optional watcher exists, so the OVERRIDDEN `update()` (UBScrollBindingCentered.as:18-23) fires synchronously during init via the same UbBatchWatcher/UbWatcherWrapper/UbExpressionWatcher chain traced for scrollController, which calls the overridden `addListeners()` (dereferencing `block`) — this is also where a wrong target type would throw (see otherStaticErrors).',
        "itemSize (part 1) and rowsCount (part 2) are still captured through the inherited watcher machinery into UBScrollBinding's PRIVATE `itemSize`/`rowsCount` fields (private, not protected — UBScrollBinding.as:24,26), but NO method this subclass overrides (`onScrollToIndex`, `onScrollNext`, `onScrollPrev`, `updateEdges`, `scrollToCenter`, `scrollToDirection`, `onScrollToName`, `onScrollChosenToCenter`, `updateControls`) references either identifier anywhere — centering instead walks `block.numChildren`/`getChildIndex`/geometry. Confirmed by grep: neither identifier appears anywhere in UBScrollBindingCentered.as. So for THIS verb, parts 1 and 2 are accepted, type-coerced, stored, and then functionally inert no matter what literal (or type) is supplied.",
    ),
    'serverTime': (
        "1 part (the only arity seen in all 76 vanilla samples for this class -- note: 76 samples in vanillaUses/vanillaSamples belong to the 'lag' worklist entry which is a separate binding; UBServerTimeBinding's own vanilla usage was not separately re-sampled here, but the class logic supports exactly this 1-part default): publishes the server-clock seconds count (stringified) under the given scope-var name, refreshed every 500ms via an internal Timer for as long as the bound DisplayObject is on stage; showLocal defaults to false so no `_day` companion var is ever written.",
        "2 parts: also publishes `<name>_day` = local day-of-week `(date.day+6)%7` (0=Monday..6=Sunday) whenever part1's literal is truthy.",
    ),
    'soundOn': (
        '1 part: init succeeds (only part0 is read), but the FIRST time the configured event fires, `onEvent` unconditionally reads expressions[1] -- RangeError #1125 at that lazy moment, not at bind time.',
        "2 parts (event, soundEventName): plays through `this.ubBlock.playSound(soundEventName)`, which requires the binding's target DisplayObject to actually BE a UbBlock instance (a `<block>` root) with a sound set already configured (`_isSoundSetSet`); on any other target, or a block with no sound set, this is a complete, silent no-op.",
        "3 parts (event, bankOrSoundSetName, soundEventName): bypasses the UbBlock requirement entirely, calling `ExternalInterface.call('sound.playSetSound', bankOrSoundSetName, soundEventName)` directly -- this is the form used in 100% (91/91) of vanilla usages sampled.",
    ),
    'stageSize': (
        'Behavior is identical for every part count, including 0 (the only form seen in all 86 vanilla uses, all `<bind name="stageSize"/>` with no value attribute): the inherited `expressions` vector is stored by UbBinding.init but this class never indexes into it anywhere (init/onAddedToStage/onRemovedFromStage/onResize/updateScope/free), so a value attribute of any length or literal type is parsed and then silently ignored.',
    ),
    'style': (
        "RUNTIME TYPE of part1 selects the whole processing path, decided fresh on every evaluation (not locked in once like `child`): `param2 is String` (UbStyleBinding.as:52) routes through `UbStyleParser.instance.process(styleKey, param2, style)`, which applies that key's own sub-grammar; any non-String runtime value (Number/uint/Boolean/Object/null) instead goes straight to a raw, completely unvalidated assignment (UbStyleBinding.as:56-58). Vanilla deliberately exploits this — e.g. passing a raw `uint` color literal (`0xFFFFCC66`) to `backgroundColor` skips `hexStyleProcessor`'s string-parsing step entirely.",
        "Per-key sub-grammar when the String path IS taken (all in UbStyleParser.as): `position`,`flow`,`textAlign`,`scrollbarAlign`,`backgroundSize` (enumStyleProcessor) accept ONLY that key's fixed label set or throw; `align` (flagsStyleProsessor, pipe-`|`-joined) silently drops any unrecognized token instead of throwing; `dropShadowFilter`/`glowFilter`/`blurFilter` (spaceSeparatedProcessor) throw if the space-separated token count exceeds the filter constructor's arity; `colorMatrixFilter` (adjustColorProcessor) and the plain numeric/dimension/boolean/hex processors never throw on a malformed string, they silently coerce to NaN/0/false.",
    ),
    'substitute': (
        '3 parts: fontCenterOffset, subValues (icon-token substitution map), textToReplace.',
        '4 parts: adds a postfix string appended after each substituted icon token.',
        '5+ parts are silently never read - UbBatchWatcher.tryToCreateOptionalWatcher only ever probes index 3.',
    ),
    'sync': (
        '0, 1 or 2 parts: the very first statement after super.init is `param1[2].eval(param3)` (UbSyncBinding.as:35) - index 2 out of range and immediately dereferenced, so init throws (RangeError #1125 or TypeError on undefined.eval) regardless of Vector OOR semantics. Parts [0]/[1] are never even reached.',
        '3 parts: two-way sync with no callback; controllerCallback stays null, onComponentChange only writes scope[propName].',
        "4 parts: part[3] is stored unevaluated (guarded by length>3, :38-41) and evaluated lazily on every accepted component change. Its runtime TYPE selects the mode (:82-89): Function -> called with zero args; anything else -> `scope.onEvent(value as String)`, a NON-bubbling (EVENT_DIRECTION_NONE) event on the binding's own scope; a non-String non-Function value becomes null via `as String` -> onEvent(null), silent.",
        '5+ parts: parts[4..] never read - silently ignored.',
        'Parts [0],[1],[2] are assigned into String-typed fields (:13,19,21), so any evaluated value is String-coerced (5->"5", {}->"[object Object]", null/undefined->null).',
        "Part[2]'s evaluated STRING is wrapped in a UbSimpleGetterExpression (UbExpressionsManager.createSimpleGetterExpression), whose eval is literally `return param1[row]` (UbSimpleGetterExpression.as:18) - a single flat bracket lookup, `scope[propName]`, not a dotted path. UbScope is a `dynamic class ... extends Proxy` (UbScope.as:11) that overrides `flash_proxy::getProperty`/`setProperty` (UbScope.as:110,228) - but AVM2 resolves a FIXED instance trait (a declared method or public var) for a bracket-notation name BEFORE ever falling through to the Proxy override, exactly as already established for this same runtime's dataRefDH scope-name part. UbScope declares, as fixed traits reachable by the exact names a bare 'propName' string can equal: methods `on` (:338), `off` (:348), `update` (:76), `newChild` (:477), `getDataRef` (:617), `incrementRefcount`/`decrementRefcount`/`copyDataFrom` etc.; public vars `$parent` (:28), `$root` (:30), `$importedFields` (:32), `$dataRef` (:36), `$sid` (:34), `$finalized` (:38); and the GETTER-ONLY `_$UbScope$_level` (:557-560, no matching `set`). A propName literal equal to any of these never reaches setProperty/getProperty at all - it hits the fixed trait directly.",
        "Read side (at INIT): `this.watcher.update()` runs synchronously at UbSyncBinding.as:44, so `scope[propName]` (UbSimpleGetterExpression.as:18) is evaluated once immediately. If propName names a method, this returns the method's Function reference (not a throw by itself); if it names a typed var, it returns that field's current value (often null before the scope tree is wired up, e.g. $parent). Either result differs from the watcher's default oldValue (undefined) or is typeof 'object' (true for both a Function and null), so onScopeChange ALWAYS fires at init (UbWatcher.as:51-58), writing that value into `obj[componentProperty]` (UbSyncBinding.as:67) - which can throw #1034 at INIT if componentProperty's declared type cannot accept it (e.g. a Function or a UbScope reference into a String-typed field), or silently succeed into a loosely-typed target.",
        "Write side (LAZY, on every accepted component change): `scope[this.propName] = _loc2_` (UbSyncBinding.as:78) targets the same fixed trait. For the getter-only `_$UbScope$_level`, this is a definite write to a read-only trait - throw #1074 per fact 5's own established rule for getter-only properties, on the first component change the binding ever accepts. For a fixed method name, this is an attempt to overwrite a non-settable trait (AVM2 raises an assignment error for this, exact code not independently measured in Scaleform). For a writable typed var ($parent/$root/$importedFields/$dataRef/$sid/$finalized), the write attempts a normal typed-property coercion - throws #1034 if incompatible, or silently corrupts the scope's own internal state (e.g. $sid, $finalized) if the type happens to match.",
    ),
    'timeFormat': (
        "1 part: format defaults to 'yyyy-MM-dd hh:mm:ss', published scope-var name defaults to '$timeString', useLocalTime defaults to false (pure UTC display, though the *_TimeZone suffix still reports '+0').",
        '2 parts: overrides the format string (part1).',
        '3 parts: overrides the scope-variable base name (part2) -- and therefore also the companion `<name>_TimeZone`/`<name>_TimeZoneName` vars it writes alongside the formatted string every update.',
        '4 parts: overrides useLocalTime (part3); a truthy non-boolean literal (any non-empty string) silently behaves as true.',
    ),
    'timeline': (
        '3 parts (minimum, and 11/16 of vanilla usage): start-event(s) descriptor, a TimelineMax vars object, and a flat tweens array.',
        '4 parts: adds a flat `events` array of position/name/data triples dispatched as scope events at specific timeline positions (5/16 of vanilla usage).',
        '5 parts: adds a `settings` object mapping scope-property-name -> TimelineMax-method-name, so changes to that scope property drive a call/setter on the timeline (unused in the sampled vanilla markup, but syntactically valid).',
        "Runtime TYPE of part0's evaluated value selects the start-trigger mode: String -> single event name; Array -> multiple event names; any other non-null/non-undefined value (a plain Object, but ALSO a boxed Number or Boolean, since AS3's `is Object` is true for both -- Number and Boolean are Object subtypes) falls into the third branch and is `for..in`-iterated as a map of scopeEvent -> methodName (or [methodName, argsArray]) invoked on the timeline instead of just calling play(); for a Number/Boolean this loop simply finds no enumerable own properties and is a silent no-op, the same mechanism the claim already correctly attributes to part4 (settings). Only a literal `null` (or undefined) fails all three `is` checks and leaves `this.startEvents` unset, so the timeline is armed but never starts -- no error either way.",
    ),
    'timestampTween': (
        '0-4 parts: throw-init - each missing index 0 through 4 is read unconditionally and in strict order, so e.g. 3 parts throws on index3 (startProps), not later.',
        '5 parts: minimum valid binding - no start/finish events are ever dispatched.',
        '6 parts: adds an onTweenStart event name, dispatched via scope.onEvent from createTransition() every time a tween actually (re)starts.',
        '7 parts: adds an onTweenFinish event name, dispatched from onTransitionFinished().',
        '8+ parts: silently ignored.',
        "runtime-TYPE (standout finding): an explicit literal `null` at index4 (endProps) throws a TypeError the FIRST time startConditions/duration/endTimestamp are simultaneously valid AND startProps (index3) is a non-empty object literal - ordinarily the binding's intended operating path, not an edge case - because setTweenPercent calls `.hasOwnProperty` on it with no null-guard inside a `for..in` over startProps; an empty-object-literal or non-object startProps masks this entirely since the loop then never iterates (see otherStaticErrors).",
    ),
    'tooltip': (
        '0-1 parts: the length>1 guard on the watcher (line 76) never fires, so no UbWatcher is created and updateScope/createTooltip never run — permanent, silent no-op; TooltipManager.registerObject is never called, so it never shows on hover.',
        "2 parts (minimum functional shape): watcher on part[1] active. Because UbWatcher.dirty starts true, watcher.update() fires INSIDE init() (UbTooltipBinding.as:79), not lazily. If part[1] is non-null at that instant (common case: 532/609 real binds), createTooltip() runs synchronously during bind construction, registering hover listeners on obj immediately; tipBehaviour defaults to 0 ('simple'). If part[1] is null at that instant (a gated ternary, also seen in vanilla), the bind stays inert until a later reactive change makes it non-null. Either way part[1] only gates the registration/hover-listening machinery and the tip's private scope data — it is NOT a direct visible show/hide switch: actual on-screen show/hide is driven independently by real mouse hover over obj (or the part[6] timer, or a Tooltip.Open/Close scope event) via TooltipManager; going back to null fully UNregisters obj (destroyTooltip→unregisterTooltip), so hover stops doing anything at all rather than merely 'staying hidden'.",
        '3+ parts: part[2] selects tipBehaviour (0-16, or a `TooltipBehaviour.<name>` lookup) — evaluated whenever createTooltip runs (eagerly or lazily per above), but the RangeError from an out-of-range numeric literal only fires later, the first time TooltipManager actually indexes `_tooltipBehaviours[behaviour]` (hover/click/resize/close; TooltipManager.as:106-122 registers exactly ids 0-16).',
        "For the 'menu' subclass only, this same slot is never evaluated at all (isExternalTipBehaviourSet is permanently true) — see the menu spec.",
        '5+ parts: part[4] overrides tipVisibleScopeName. An explicit literal here — including `null` — is NOT equivalent to omitting the part; see the parts table and otherStaticErrors for the confirmed null-vs-omit trap.',
        '6+ parts: part[5] overrides tipStateScopeName (any literal accepted, never throws).',
        '7+ parts: part[6] truthy triggers a 50ms auto-open timer instead of waiting for a hover/open trigger.',
        '8+ parts: trailing parts are compiled but never read by any code in this class — silently inert.',
    ),
    'transition': (
        "0 parts: throw-init - updateData's first statement reads index1 of an empty vector.",
        '1 part: throw-init - same line (index1 still out of range).',
        "2 parts: throw-init - index1 succeeds but processPropsForOut's read of index2 is out of range.",
        "3 parts: ALWAYS throw-init regardless of index2's literal - if non-null, the next line's read of index3 (propsForIn) is out of range; if exactly null, the same missing index3 is hit one statement earlier inside processPropsForOut's derive-fallback.",
        '4 parts: minimum valid transition - customBack false, easing defaults to 0, delay defaults to 0.',
        '5 parts: presence ALONE (any value/type, including null) sets customBack=true, so propsForOutCustom (index4) replaces propsForOut (index2) for every out-transition from then on.',
        '6 parts: adds an easing-id override.',
        '7 parts: adds a delay override; matches the vanilla maximum (85 samples).',
        '8+ parts: silently ignored - nothing reads index7 or beyond.',
    ),
    'ubFocus': (
        "Pure optional-argument growth (1→4 parts progressively enables defaultFocused/focusable/focusOnClick watchers). No branch depends on the RUNTIME TYPE of any part: every part is force-coerced into a primitive target type (int for tabIndex, Boolean for the other three), and ActionScript's ToInt32/ToBoolean coercions never throw regardless of the source type — a non-numeric/non-boolean-shaped literal just silently becomes an unexpected 0/false/true rather than erroring.",
    ),
    'vTileHack': (
        'Identical to stageSize: `expressions` is stored (inherited) but never indexed anywhere in this class (init/onFrame/free), so any part count or literal type is accepted and silently ignored. All behavior comes from an ENTER_FRAME poll of `getBounds()`, not from the bind value.',
    ),
    'var': (
        'Only ever indexes part[0]; behavior branches on the RUNTIME TYPE of part[0]\'s evaluated value inside UbScope.copyDataFrom() (UbScope.as:576-615, read verbatim): getQualifiedClassName==\'Object\'/\'Dictionary\' -> `for(k in param1) setProperty(k, param1[k])`, one scope prop per own key (397/397 vanilla samples use this shape, e.g. `{ isOnTop: false, isSceneUnlaged: false }`); ==\'Array\' -> `setProperty(\'dataArray\', param1)`; int/uint/String/Number/Boolean -> `setProperty(\'$value\', param1)`; `Object(param1).isVector()` -> also \'dataArray\'; else -> UbReflector.getPublicVariables() copies each public var by name. A bare literal like `<bind name="var" value="5"/>` therefore creates `scope.$value`, not a field named \'5\'.',
        "`var` is IMMUNE to the UbScope fixed-trait/Proxy-precedence hazard that afflicts watch/contains/indexOf/concat's name-parts (see those verbs' specs): every branch of copyDataFrom writes via `this.setProperty(key, value)` - a normal instance-method call resolvable only because UbScope.as itself opens `use namespace flash_proxy;` (UbScope.as:9) - never the generic `scope[key] = value` dynamic-property OPERATOR that all four other bindings use from OUTSIDE UbScope's package. A direct method call always executes the method body (unconditionally writing `$storage[key]=value`); it is not subject to the AVM2 rule that a fixed/declared trait short-circuits Proxy.setProperty for the generic get/set operator. So an object-literal key equal to a real UbScope member name (e.g. `{ $parent: 5 }`) is completely inert: it lands only in `$storage['$parent']`, invisible to every ordinary read of `scope.$parent` / `scope['$parent']` (both resolve the real fixed trait first) - a harmless orphaned key, never a throw or a corruption, unlike the other four verbs.",
    ),
    'visible': (
        'None — there is no length check at all; any part count >=1 behaves identically, and any part beyond index0 is never read.',
    ),
    'watch': (
        'Exactly 2 parts are ever indexed (name, value); a 3rd+ split part is silently never read. Matches all 5215 vanilla samples, which are all part-count 2.',
        "part[0] (the target property name) is evaluated exactly ONCE, up front at init (line 24) and never again - so if it isn't a literal string, whatever it evaluates to on the FIRST pass is the property name for the lifetime of the binding, even though part[1] (the value) is fully reactive/watched.",
    ),
}
